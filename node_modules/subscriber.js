// Observer
class Subscriber {
    constructor(name) {
        this.name = name;
    }

    notify(eventName, day) {
        console.log(`${this.name}, there is an event of ${eventName} will be ${day}.`);
    }
}

module.exports = Subscriber;