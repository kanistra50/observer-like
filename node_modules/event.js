// Observable Object
class  Event {
    constructor(name) {
        this.name = name;
        this.subscribers = []
    }

    subscribe(observer) {
        this.subscribers.push(observer);
    }

    event(day) {
        this.subscribers.forEach(observer => observer.notify(this.name, day))
    }
}

module.exports = Event;